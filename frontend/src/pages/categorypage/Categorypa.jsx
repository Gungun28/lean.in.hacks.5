import React from 'react'
import Category from '../../components/category/category'

const Categorypa = ({type}) => {
  return (
    <div>
        <Category type={type}/>
    </div>
  )
}

export default Categorypa